NODE_ENV=production
APP_VERSION=dev

# PostgreSQL：生产环境必须替换默认密码。
POSTGRES_DB=ai_ops
POSTGRES_USER=ai_ops
POSTGRES_PASSWORD=change_me_with_a_strong_password

# 现有智能客服模型与 RAG 接口；未配置时使用确定性 Mock，便于演示。
CSS_LLM_API_URL=
CSS_RAG_API_URL=
CSS_API_TOKEN=
